/**
 * Properties of modulo (%)
 * (a+b) % m = (( a % m ) + ( b % m )) % m
 * (a-b) % m = ( (a % m ) - ( b % m ) + m ) % m
 * (a * b) % m = ( (a % m ) * ( b % m )) % m
 * (a/b) % m = (( a % m ) * ( b^-1 % m )) % m
 * ( a % m ) % m = a % m
 * 
 * If p is a prime number which is not a divisor of b the ab % p = a % p 
 *  
 */